# MDviewer 변경 이력 (History)

이 문서는 MDviewer 프로젝트의 개발 및 기능 변경 이력을 날짜별로 기록합니다.
(Git 커밋/등록 시마다 해당 날짜를 기준으로 변경 내역을 상단에 추가합니다.)

---

## 2026-09-21

### 추가 및 신기능 (Added)
- **다단 보기 (Multi-Column View)**: 우클릭 메뉴에 `다단 보기` 하위 메뉴 추가 (`1단 (기본)`, `2단`, `3단`, `자동 (너비 맞춤)`). 와이드 모니터에서 글줄이 너무 길어지는 문제를 해결하고 책이나 잡지처럼 나란히 읽을 수 있는 뷰 제공.
- **1단 슬라이드 휠 스크롤 (A방식)**: 휠을 아래로 굴릴 때마다 1개 단 너비만큼 부드럽게 왼쪽으로 슬라이드 이동하여 시선의 흐름이 자연스럽게 이어지도록 구현 (`[1|2|3]` → `[2|3|4]`). 키보드 좌/우 방향키(1단 이동), PageUp/Down(화면 전체 이동), Home/End(처음/끝) 지원.
- **다단 텍스트 연속 드래그 선택**: CSS Native Multi-Column 기반 레이아웃을 적용하여 단 경계를 가로지르는 마우스 드래그 선택 및 Ctrl+C 복사가 끊김 없이 하나의 문장으로 이어지도록 보장.
- **요소 분할 방지**: 표(Table), 코드 블록, 이미지, 수식(KaTeX), 인용문, 경고창 등이 단 경계에서 잘리지 않도록 `break-inside: avoid` 처리.
- **설정 영속화**: 선택한 다단 보기 모드를 `AppData/MDviewer/columns.txt`에 저장하여 다음 실행 시에도 유지.
- **Word 변환 및 편집 모드 격리**: Word 저장 시에는 단일 열 표준 DOM으로 안전하게 렌더링 후 변환하여 서식 깨짐을 방지하며, 편집 모드 진입 시에는 커서 동기화를 위해 단일 열 뷰로 자동 전환.

### 변경 및 개선 (Changed)

- **블루스카이 바탕색 흰색 고정**: 테마의 전체 배경과 미리보기 바탕은 흰색으로 유지하고, `#CEDEF2`는 코드·인용·표 헤더 등 보조 영역에만 사용.
- **블루스카이 팔레트 재조정**: `#CEDEF2` 배경, `#A0C4F2` 경계, `#6DA7F2` 포인트, `#035AA6` 강조 글자, `#023373` 본문으로 재구성해 밝은 하늘색과 높은 텍스트 대비를 분리.
- **블루스카이 강조 글자색 조정**: 제목·코드·표 헤더 등 강조 텍스트를 `#03045E`로 변경해 포레스트 그린과 시각적으로 구분.
- **연결 등록 안내 버튼 테마색 적용**: `.md` 연결 프로그램 등록 팝업의 `기본 앱 설정 열기` 버튼이 블루스카이·포레스트 그린에서도 각 테마의 포인트 색상을 사용하도록 수정.
- **알바트로스 팔레트 변경**: 사용자 지정 색상 `#FAFAF8`, `#DADADA`, `#B0B3B8`, `#43464B`, `#0F1012`를 적용해 모던 뉴트럴 톤으로 통일.
- **블루스카이 팔레트 변경**: 사용자 지정 색상 `#CAF0F8`, `#90E0EF`, `#00B4D8`, `#007786`, `#03045E`를 적용해 포인트·제목·본문·테두리·보조 배경 색상을 통일.
- **.md 연결 프로그램 등록 기능 복원**: 홈 화면에서 HKCU 레지스트리에 `.md`/`.markdown` 연결을 등록하고, `assoc_guide.png` 안내 그림과 Windows 기본 앱 설정 창을 여는 다이얼로그를 복원.
- **탭 제목 글꼴 크기 확대**: 상단 탭의 가독성을 높이기 위해 탭 이름 글꼴 크기를 8pt에서 9pt로 상향 조정(`Brand.Ui(9f)`).
- **README 이미지 링크 수정**: GitHub README의 스크린샷 링크를 저장소 기준 상대경로와 URL 인코딩된 파일명으로 정리하고, Screenshots 폴더의 이미지 5개를 Git 추적 대상으로 추가.
- **코드 블록 줄 번호 중복 수정**: 언어가 지정된 코드 블록(예: bat)이 일반 코드 블록 처리기에 다시 렌더링되던 문제를 수정해 1번 줄이 중복 표시되지 않도록 개선.

---

## 2026-09-18

### 삭제 (Removed)
- 홈 화면의 `md 파일 연결프로그램 등록` 기능과 Windows 레지스트리 등록 코드, 기본 앱 안내 창 및 전용 이미지 리소스 삭제.
- 기존 홈·서강 테마·알바트로스 테마·편집 모드 스크린샷 4개 삭제. README의 스크린샷 섹션과 존재하지 않는 이미지 참조도 제거.

### 변경 및 개선 (Changed)

- **홈 GitHub 링크**: 홈 화면에 `GitHub 저장소` 링크를 추가하고 클릭하면 시스템 기본 웹 브라우저로 프로젝트 저장소를 열도록 함. 현재 테마의 색상과 글꼴을 적용하고 기존 홈 링크 아래에 배치.
- **VIEW 직접 클릭 이동 복원**: 편집 중 사용자가 오른쪽 본문을 직접 왼쪽 클릭했을 때만 해당 원문 줄로 커서·포커스 이동. 자동 스크롤/갱신/이미지 로딩의 역방향 동기화는 계속 차단. 문서의 논리 줄 번호를 사용해 자동 줄바꿈·표 앞 정규화에도 위치를 맞추고, 클릭한 VIEW는 다시 스크롤하지 않음.
- **편집 커서 이동 오류 수정**: 미리보기의 자동 스크롤 위치를 편집기로 되돌려 보내던 경로 제거. 자동 동기화는 편집기 → VIEW 방향으로만 수행하며, 이미지의 늦은 로딩·미리보기 갱신·스크롤이 커서/선택 영역/입력 위치를 변경하지 않도록 함. 이미 큐에 들어간 구형 위치 메시지도 무시.
- **홈에서 샘플 문서 열기**: `test-sample.md 열기` 링크 추가. 샘플을 실행 파일에 내장하고, 실행 파일 폴더에 파일이 없을 때만 생성한 뒤 VIEW로 열기. 기존 파일은 내용·수정 시각을 보존. 링크에 테마색·글꼴 적용.
- **메뉴 체크 표시 개선**: 기본 하늘색 사각형 체크 배경/테두리를 없애고 현재 테마색의 체크 선만 직접 그림. 서강 글꼴의 기호 지원 여부와 관계없이 표시되며 상위/하위 메뉴에 동일 적용.
- **탭 밑줄 중복 제거**: 선택 탭에 그리던 3px 밑줄을 제거해 탭 아래 2px 공통 테마선만 표시. 선택 탭은 굵은 글씨와 테마색으로 구분.
- **상단 테마선 추가**: 열린 문서의 탭 바로 아래에 하단 띠와 동일한 테마색의 2px 선을 표시. 검색·강조 바, 편집·분할 모드와 창 크기 변경에도 상단 위치·전체 너비 유지.
- **테마 변경 후 잘못된 수정 표시 수정**: 파일의 CRLF/CR 줄바꿈과 RichTextBox의 LF 줄바꿈이 다르게 비교되던 원인 수정. 파일 로딩 후 편집기 텍스트를 수정 비교 기준으로 사용하여 테마 변경만으로 `*`가 붙지 않도록 함. 파일 바이트와 실제 미저장 편집 상태는 보존.
- **테마 전환 클릭 수정**: 전역 마우스 필터가 테마 하위 메뉴를 상위 메뉴 바깥 클릭으로 판단해 선택 전에 닫던 오류 수정. 열린 하위 메뉴까지 재귀적으로 영역 검사. 실제 팝업 위치에 맞는 클릭 경로로 수정.
- **테마 하위 메뉴**: 우클릭 메뉴의 테마 4개를 `테마` 하위 메뉴로 묶음. 선택 체크는 하나만 유지하며 현재 테마를 다시 눌러도 해제되지 않음. 하위 메뉴에도 테마 글꼴 적용.
- **포레스트 그린 테마 추가**: 우클릭 → 테마 하위 메뉴에서 선택. 흰 배경, 짙은 초록 제목·링크·체크박스·현재 줄 삼각형, 연한 초록 표 머리글·인용문, 서강 글꼴 적용. 테마 설정 저장·복원 및 VIEW 기반 Word 변환에 반영.
- **VIEW 기반 Word 변환**: Markdown 원문을 별도로 파싱하던 변환기를 실제 VIEW DOM 및 계산된 스타일 기반으로 교체. 제목, 강조, 취소선, 목록·체크 상태, 표, 링크, 코드 줄바꿈, 테마 글꼴·색상·문단 간격을 편집 가능한 Word 요소로 변환. 이미지와 렌더링된 수식은 내장 이미지로 보존. 화면 밖 본문과 이미지도 포함.
- 저장 직전 최신 미저장 내용을 VIEW에 렌더링하고 로딩 완료를 기다린 뒤 변환. JSON은 VIEW에서 정렬된 형태로 저장. 복사 버튼·편집 표시·줄 번호는 제외. 변환 성공 후 대상 파일을 교체해 변환 실패 시 기존 파일 보존.
- **블루스카이 서강 글꼴**: VIEW 본문, 홈, 메뉴, 검색·강조 UI에 서강 글꼴 적용. 흰색 배경과 파란색 포인트는 유지.
- **Word 저장 시 VIEW 전환**: Word 저장 메뉴와 Ctrl+Shift+S 실행 시 저장 창을 열기 전에 편집·분할을 해제하고 단일 VIEW 모드로 전환. 미저장 내용은 유지하며 메뉴 체크도 동기화.
- **편집 모드 현재 위치 표시 변경**: VIEW 왼쪽의 회색 전용 여백에 작은 삼각형으로 표시. 상단 `편집` 라벨은 제거.
- 삼각형은 테마 색상을 따르며 커서 이동, 스크롤, 확대, 크기 변경 및 미리보기 갱신에 맞춰 위치를 다시 계산. 현재 위치가 화면 밖으로 나가거나 편집 모드를 종료하면 숨김.
- 표시 영역은 마우스 클릭과 텍스트 선택을 방해하지 않으며, 확대 배율과 관계없이 작은 크기를 유지.

- **분할 비율 조절**: 세로·가로 경계선에 드래그 손잡이 추가. 탭별·방향별 비율을 기억해 창 크기 변경, 검색 바 표시, 편집 전환 및 분할 재활성화 시 유지. 더블클릭으로 50:50 복원.

- **편집 종료 동작 통일**: F8 및 우클릭 메뉴로 편집 모드를 종료하면 세로·가로 분할을 모두 해제하고 단일 뷰로 전환. 분할 메뉴 체크 상태도 함께 해제.
- **블루스카이 배경**: 미리보기 문서 및 앱 기본 배경을 흰색으로 변경.

- **Word 표 변환 보완**: 렌더링 DOM의 행 사이 공백을 셀로 처리하지 않도록 수정해 실제 셀 수와 테마 테두리 색상을 보존.

---

## 2026-09-17

### 개선 및 수정 (Improved & Fixed)
- **편집 모드 커서 위치 우측 미리보기 뷰어 실시간 강조 표시 (Active Line Indicator) 및 논리 줄 번호(Logical Line) 동기화**:
  - **자동 줄바꿈(WordWrap) 환경에서의 줄 번호 불일치 완전 해결**: 긴 문장이 에디터 폭에 의해 여러 줄로 자동 개행(Soft wrap)될 때, `RichTextBox`가 시각적 줄 번호를 반환하여 Markdig AST의 실제 마크다운 소스 줄 번호와 어긋나던 문제를 완벽히 해결.
  - 실제 개행문자(`\n`) 기반의 논리 줄 번호(`GetLogicalLine`)를 산출하여, 에디터 거터(Gutter)의 줄 번호 표시와 미리보기의 `data-src-line` 블록이 1:1로 100% 정확하게 일치하도록 구현.
  - 에디터 좌측 거터(Gutter) 역시 VS Code / Visual Studio와 동일하게 자동 줄바꿈된 연속 줄에는 번호를 비워두고 첫 시작 줄에만 실제 줄 번호를 렌더링.
  - **테마별 악센트 바 및 은은한 배경 워시**:
    - 서강 테마: 카디널 레드 좌측 인디케이터 바 + 은은한 레드 워시 (`#b3292e`, `rgba(179,41,46,0.08)`)
    - 블루스카이 테마: 스카이 블루 좌측 인디케이터 바 + 소프트 블루 워시 (`#0284c7`, `rgba(2,132,199,0.09)`)
    - 알바트로스 테마: 다크 차콜 좌측 인디케이터 바 + 흑백 워시 (`#333333`, `rgba(0,0,0,0.06)`)
  - 타이핑, 방향키 이동, 마우스 클릭 등 모든 커서 변경 이벤트에서 딜레이 없이 부드러운 전환(`transition`)으로 현재 편집 중인 위치를 직관적으로 파악 가능하며, 뷰어 전용 모드 전환 시에는 자동으로 깔끔하게 정리.
- **테마별 대화형 태스크 체크박스(Task List Checkbox) 악센트 컬러 동기화**:
  - 서강 테마(`Sogang`): 고유 카디널 레드(`Cardinal Red`, `#b3292e`) 체크박스 악센트 적용.
  - 블루스카이 테마(`Blue Sky`): 청량한 스카이 블루(`Sky Blue`, `#0284c7`) 체크박스 악센트 적용.
  - 알바트로스 테마(`Albatross`): 모던한 다크 차콜(`Dark Charcoal`, `#222222`) 체크박스 악센트 적용.
  - CSS `accent-color` 속성을 각 테마의 고유 포인트 컬러와 완벽히 일치시켜 일관되고 깔끔한 UI 경험 제공.
- **태스크 체크박스(Task List) 클릭 시 화면 재렌더링 및 벌렁거림(Flicker) 완전 제거**:
  - 미리보기에서 체크박스 클릭 시 전체 HTML 재교체(`innerHTML`) 및 KaTeX 수식 재실행으로 인해 발생하던 화면 번쩍임/스크롤 재계산 현상(벌렁거림)을 완벽히 해결.
  - 체크박스 클릭 시 `_suppressPreviewRefresh` 플래그를 적용하여 백그라운드 Markdown 소스(`[ ]` <-> `[x]`)와 탭 수정 상태(`*`)만 0ms로 즉시 동기화하고, 뷰어는 리로드 없이 네이티브 체크 상태를 유지하여 렉/튐 없는 무지연 인터랙션 구현.
- **검색 바(Find Bar) 및 강조 키워드 바 높이 대폭 확장 (44px / 42px)**:
  - `찾기:` 라벨, 검색 텍스트 상자, 버튼 및 `단어단위`/`모든 탭` 체크박스의 하단 테두리가 패널 경계선에 닿거나 글꼴이 짤리는 문제를 완전히 해소하기 위해 `_findBar` 높이를 **44px**로, `_hlBar` 높이를 **42px**로 대폭 확장.
  - 상하 7~8px의 여유로운 패딩을 부여하여 어떤 디스플레이 배율(DPI)에서도 하단이 절대 잘리지 않는 쾌적한 레이아웃 보장.
- **신규 '블루스카이 (Blue Sky)' 테마 추가**:
  - 세련되고 청량한 파란색 계열의 '블루스카이 테마' 신규 탑재 (우클릭 컨텍스트 메뉴에서 `블루스카이 테마` 선택 지원).
  - **테마 디자인 구성**:
    - **배경 및 워시**: 청명한 아이스 블루 워시(`AliceBlue`, `#f0f8ff`).
    - **포인트 및 강조색**: 스카이 블루(`Sky Blue`, `#0284c7`), 사파이어 네이비 헤더(`Navy`, `#0369a1`), 딥 슬레이트 잉크 텍스트(`#0f172a`).
    - **인용구 및 코드/표**: 스카이 블루 악센트 보더(`#bae6fd`), 소프트 블루 코드 배경(`#e0f2fe`), 다크 슬레이트 코드 블록(`#0f172a`).
    - **하단 밴드 및 탭 바**: 활성 탭 스카이 블루 인디케이터 및 네이비 탭 텍스트, 하단 스카이 블루 포인트 바 동기화.
  - 테마 설정(`theme.txt`)에 자동 저장되어 프로그램 재시작 시에도 유지.
- **Ctrl+F 재입력 시 찾기 바 닫기(Toggle) 및 모든 탭 동시 닫기 지원**:
  - 검색 바가 열려 있는 상태에서 `Ctrl + F`를 다시 누르거나 `ESC` / `X` 버튼 클릭 시 현재 탭 및 열려 있는 모든 탭의 찾기 바를 즉시 닫고 하이라이트를 정리하도록 토글(Toggle) 기능 구현.
  - 검색 텍스트 박스(`_txtFind`)에 포커스가 있을 때도 `Ctrl + F`로 즉시 닫기 가능.
- **이미지 Alt 캡션(`![alt](url)`) 검색 및 이전/다음 하이라이트 동기화**:
  - Markdown 이미지의 대체 텍스트(예: `![MDviewer 홈 화면](스크린샷_Home.png)`)가 HTML 속성(`alt="..."`)으로만 존재하여 웹 뷰어 검색(DOM TextNode 순회)에서 제외되고 일치 개수가 어긋나던 문제 해결.
  - HTML 변환 시 이미지를 시맨틱 `<figure class="md-img-figure"><img ...><figcaption class="md-img-caption">alt</figcaption></figure>` 구조로 렌더링하여 alt 텍스트가 DOM 텍스트 노드로 생성되도록 개선.
  - 이를 통해 `화면` 등 alt 텍스트 내 단어가 에디터와 웹 뷰어 양쪽 모두에서 정확한 인덱스로 검색되고, 주황색 활성 하이라이트 및 부드러운 스크롤 이동이 완벽하게 연동되도록 지원.
- **WinForms BeginInvoke 마샬링 예외(TargetParameterCountException) 수정**:
  - `RenderMarkdownCore` 내부에서 인자 2개를 받는 `ApplyWebZoom(WebView2, double)` 메서드 그룹이 `BeginInvoke(ApplyWebZoom)`으로 잘못 전달되어 런타임에 발생하던 `System.Reflection.TargetParameterCountException: Parameter count mismatch` 오류 수정.
  - 모든 `BeginInvoke` 호출부를 명시적 람다 `Action`으로 표준화하여 안전한 스레드 마샬링 보장.
- **통합 검색 바(Find Bar) 및 편집 모드 / 모든 탭 연계 검색 구현**:
  - `Ctrl + F` 입력 시 편집 모드(에디터) 및 뷰어 화면 상단에 일관된 통합 검색 바(Find Bar) 노출.
  - **이전/다음 탐색**: `◀ 이전` (Shift+Enter / Shift+F3), `다음 ▶` (Enter / F3) 버튼 및 일치 카운트(`1/14` 등) 실시간 표시.
  - **단어 단위 일치 (Whole Word)**: 체크박스 On/Off로 단어 경계(`\b`) 일치 검색 지원.
  - **모든 탭 검색 (All Tabs)**: 현재 문서의 끝에 도달하면 다음 열린 탭으로 자동 이동하여 검색을 이어가며, 마지막 탭에 도달하면 다시 첫 번째 탭으로 순환 래핑(Wrap-around) 탐색 지원.
  - **에디터 & 뷰어 실시간 동기화**: 편집 모드에서는 에디터 선택 영역(`Selection`)과 커서 스크롤이 즉시 이동하며, 웹 뷰어에서도 노란색/주황색 하이라이트(`mark.md-find-hl` / `mark.md-find-active`)로 현재 위치를 시각적으로 강조.
- **화면 확대/축소(Zoom) 배율 OSD 표시 및 분할 화면 개별 줌(Ctrl+Shift+Wheel) 지원**:
  - `Ctrl + 휠`: 활성 분할 화면(에디터 및 뷰어)을 동시에 확대/축소하며, 각 분할 화면 우측 상단에 현재 배율(`100%`, `125%` 등)을 반투명 플로팅 뱃지로 약 1.1초간 부드럽게 표시.
  - `Ctrl + Shift + 휠`: 마우스 커서가 위치하거나 클릭한 해당 분할 화면(좌측 에디터 또는 우측 미리보기 뷰어)만 독립적으로 확대/축소하고, 해당 분할 영역 우측 상단에만 배율 변화를 표시.
- **편집창(에디터) 폰트 및 줄 간격 동기화**:
  - 편집 모드의 좌측 에디터(`RichTextBox`) 폰트를 인라인 코드와 동일한 `Consolas` (크기 `10.5pt` / `.88em` 상당)로 통일.
  - Win32 `EM_SETPARAFORMAT` (`PARAFORMAT2`)을 연동하여 에디터 텍스트에도 코드 블록 및 인라인 코드와 동일한 **`line-height: 1.4`** 비례 줄 간격 자동 적용.
- **코드 블록 및 문서 본문 줄 간격(Line Spacing) 최적화**:
  - `<pre>` 요소 내 블록(`display: block`) 줄 번호 래퍼와 개행(`\n`)이 중복되어 발생하던 코드 블록 이중 줄간격(Double spacing) 현상 수정.
  - 코드 블록 라인 높이(`line-height: 1.4`, 폰트 크기 `13px`) 및 여백을 최적화하여 촘촘하고 가독성 높은 개발자 친화적 뷰 제공.
  - 본문 및 단락 줄 간격(`line-height: 1.6`, `p/ul/ol` 여백)도 컴팩트하게 조정.
- **코드 블록 라인 넘버(Line Numbers) 표시 및 복사/선택 제외 처리**:
  - 모든 Markdown 코드 블록(```` ``` ```` 및 JSON 뷰어)의 각 라인 좌측에 줄 번호(Line Number) 표시.
  - 줄 번호는 CSS `user-select: none; pointer-events: none;` 및 자바스크립트 분리 추출을 적용하여 마우스 드래그 선택(`Ctrl+C`) 및 헤더의 `Copy`/`Copy JSON` 버튼 클릭 시 복사 대상에서 완벽히 제외되도록 구현.
- **Ctrl+F8 단어 강조 토글 (Add / Remove Toggle)**:
  - `Ctrl+F8` (또는 `Shift+F8`) 1회 입력 시 선택 단어를 강조 키워드에 추가하고, 이미 강조 목록에 등록된 단어일 경우 목록에서 즉시 삭제(제거)하도록 토글 기능 구현.
- **F8 편집 모드 및 화면 분할 On/Off 토글**:
  - `F8` 단축키를 눌러 편집 모드(세로 분할)로 즉시 진입하고, 편집 모드에서 다시 `F8`을 누르면 편집 모드 종료 및 화면 분할이 취소(단일 뷰어로 복귀)되도록 개선.
- **JSON 자동 포맷팅(Prettify) 및 One Dark Pro 구문 하이라이팅(Syntax Highlighting)**:
  - 독립 JSON/JSONC 파일 및 Markdown 코드 블록(````json ... ````)에 대해 자동 Pretty-print(들여쓰기 2칸 정렬) 및 One Dark Pro 테마 기반 토큰별 구문 강조 구현:
    - Key(속성명): `#61afef` (Cyan/Blue)
    - String(문자열): `#98c379` (Green)
    - Number(숫자): `#d19a66` (Orange)
    - Boolean(참/거짓): `#e06c75` (Coral/Red-Pink)
    - Null: `#e5c07b` (Amber/Italic)
    - Punctuation(`{}[]:,` 기호): `#abb2bf` (Dimmed Gray)
  - 코드 블록 상단 헤더 바에 언어 태그(`JSON` / `CODE` / 언어명) 및 원클릭 복사(`Copy JSON` / `Copy`) 버튼 제공 (`Copied!` 피드백 및 클립보드 fallback 지원).
  - 잘못된 JSON 문법(Syntax Error) 입력 시 에러 배지와 줄 번호/오류 원인을 안내하는 안전한 폴백 렌더링 지원.
- **LaTeX 수식(Math) 렌더링 지원 (KaTeX 연동)**:
  - 인라인 수식(`$...$` 또는 `\(...\)`) 및 블록 수식(`$$...$$` 또는 `\[...\]`)이 텍스트로만 남던 문제를 해결하고, 고성능 KaTeX 수식 렌더러를 연동하여 적분 기호($\int$), 분수($\frac{a}{b}$), 윗첨자/아랫첨자($x^2$), 이탤릭 수학 기호 등이 표준 수식 타이포그래피로 미려하게 렌더링되도록 구현.
  - 편집 모드에서 수식 작성 및 수정 시에도 실시간 수식 재렌더링(`renderMath`) 자동 적용.
- **편집 모드 에디터 커서-미리보기 실시간 위치 동기화 및 양방향 클릭 연동**:
  - 편집기에서 커서 이동(마우스 클릭, 방향키 이동, 텍스트 입력, 엔터 키 입력 등) 시 코드 블록(Fenced Code Block) 및 모든 마크다운 요소를 우측 뷰어 화면의 최적 위치(상단 35% 영역)로 자동 스크롤하여 항상 즉시 보이도록 개선.
  - 우측 미리보기 뷰어에서 코드 블록(`<pre><code>`), 제목, 본문, 목록, 표 등 임의의 요소를 클릭했을 때 좌측 에디터가 해당 소스 코드 줄로 즉시 스크롤되고 커서 및 포커스가 이동하는 양방향 클릭 이동 기능 구현.
  - 마크다운 블록 간 줄 번호 선형 보간(Linear Interpolation) 알고리즘을 구현하여 연속 엔터(Enter) 입력이나 코드 블록 내부 줄 클릭 시에도 스크롤 위치가 정확하고 매끄럽게 추적 반영되도록 개선.
  - 편집 중 깜빡임 없는 고속 실시간 렌더링을 위해 전체 페이지 재로드(`NavigateToString`) 대신 DOM 내부 갱신(`innerHTML`) 및 체크박스/키워드 강조 자동 재연결 지원.
  - 에디터 스크롤 동기화 시 커서 위치가 임의로 초기화되는 현상을 방지하기 위해 Win32 `EM_GETFIRSTVISIBLELINE` 및 `EM_LINESCROLL` 메시지 기반의 비파괴 스크롤 제어 적용.

---

## 2026-09-15

### 추가 (Added)
- **선택 단어 강조 기능 (Shift + F8)**:
  - 탭 하단에 `강조키워드:` 입력란을 추가하고, 선택된 단어를 `Shift + F8` 단축키로 강조 단어에 등록하여 문서 전체에서 선명한 형광 노란색(`#ffff00`)으로 일괄 강조 표시하는 기능 구현. (볼드 효과 미적용)
  - 평상시에는 입력창이 숨겨져 있으며, 우클릭 메뉴의 `강조 키워드` 항목 또는 `Shift + F8` 단축키 사용 시에만 나타나도록 개선.
  - 띄어쓰기로 구분된 키워드 실시간 추가/삭제/수정 지원 및 테마 무관 고정 형광 컬러 적용.
  - 홈 화면 단축키 목록 및 README.md에 `Shift + F8` 단축키 및 주요 기능 설명 추가.
- **우클릭 메뉴 신규 그룹 추가**:
  - `닫기` 그룹과 `편집 모드` 그룹 사이에 `검색 (Ctrl+F)`, `강조 키워드 (Shift+F8)` 메뉴 그룹 신설.
- **우클릭 서브 메뉴 닫기 기능**: 컨텍스트 메뉴에 현재 활성 탭을 닫을 수 있는 `닫기` 메뉴 항목 추가.
- **단일 인스턴스(Single Instance) 탭 열기**:
  - Windows 탐색기에서 `.md` 파일을 더블 클릭하거나 연결 프로그램으로 실행할 때 새 창이 중복 실행되지 않고, 기존에 열려 있는 MDviewer 창의 새 탭으로 열리도록 IPC(Named Pipe) 및 Mutex 기반 프로세스 간 통신 구현.
  - 기존 창이 최소화되어 있는 경우 정상 크기로 복원 및 최상위 활성화(Foreground) 처리.
- **프로젝트 관리 가이드 문서 추가**:
  - `git.md`: Git 커밋 및 변경 이력 관리 원칙 가이드 작성.
  - `History.md`: 일자별 변경 이력 문서 신규 작성.

### 변경 및 개선 (Changed)

- **홈 빌드 날짜 표시**: 두 번째 줄에 `last build : YYYY.MM.DD`를 표시하고 빌드 시 자동 갱신. 빈 줄 다음에 파일 연결 프로그램 등록 링크 배치.
- **기본 앱 설정 안내 유지**: `기본 앱 설정 열기` 버튼의 너비 및 자동 크기 조정을 적용하고, 클릭 후 안내 창이 닫히지 않도록 수정. 사용자가 직접 닫기 가능.
- **GitHub 홈 스크린샷 추가**: README에 홈, 서강 테마, 알바트로스 테마, 편집 모드 스크린샷 4장 추가.
- **기본 앱 등록 안내 다이얼로그 개선**: 홈 화면의 `md 파일 연결프로그램 등록` 실행 시 Windows 11 기본 앱 설정 화면 가이드 스크린샷(`스크린샷_기본앱등록.png`)이 포함된 전용 모달 안내창을 띄우고 `기본 앱 설정 열기` 버튼을 제공하도록 개선.
- **단어 검색 안내 추가 (Ctrl + F)**: 홈 화면 단축키 목록 및 README.md에 단어 검색(`Ctrl + F`) 단축키 안내 추가. (WebView2 내장 검색 활용)
- **외부 웹 브라우저 연동**: Markdown 문서 내의 `http://`, `https://`, `mailto:` 등 웹 URL 링크 클릭 시 뷰어 내부 네비게이션을 차단하고 Windows 기본 전용 브라우저에서 열리도록 개선. (로컬 `.md` 상대 경로 링크는 뷰어 내 새 탭으로 유지)
- **서브 메뉴 닫힘 UX 개선**: 우클릭 서브 메뉴가 열려 있는 상태에서 웹 뷰, 에디터, 탭, 배경 등 화면 어디든 마우스 클릭 시 메뉴가 즉시 닫히도록 개선.
- **탭 글자 크기 조정**: 탭 제목 폰트 크기를 기존 7pt에서 8pt로 1포인트 상향하여 가독성 개선.
- **첫 화면 설명 갱신**: 홈 화면 단축키 목록에 `- Ctrl + F  단어 검색`, `- Shift + F8  단어 강조` 추가 및 우측 마우스 안내 영역에 `- 탭 휠클릭  탭 닫기` 설명 추가.
- **README.md 문서 최신화**: 홈 화면 및 서브 메뉴에서 제공하는 전체 단축키, 마우스 조작법, 컨텍스트 메뉴, 주요 기능 상세 설명 추가.

---

## 2026-09-10

### 신규 개발 (Initial Release)
- **MDviewer 초기 버전 개발**:
  - 서강대학교 MOT대학원 데이터 엔지니어링 프로그래밍 수업용 Windows Markdown 뷰어 & 편집 프로그램.
- **핵심 기능 구현**:
  - Microsoft Edge WebView2(Chromium) + Markdig 기반 실시간 Markdown 렌더링.
  - 편집 모드 및 소스 에디터(RichTextBox) + 줄 번호 거터(Gutter) 지원.
  - 세로 분할 / 가로 분할 뷰 모드 및 에디터-미리보기 실시간 스크롤 동기화.
  - DocumentFormat.OpenXml 기반 Word(`.docx`) 파일 내보내기/저장 기능.
  - 드래그 앤 드롭을 통한 다중 탭 문서 열기 지원.
  - 서강 테마(시그니처 레드/와인) 및 알바트로스 테마(모던 모노톤) 지원.
  - Windows 레지스트리 기반 `.md` 파일 연결 프로그램 등록 지원.
