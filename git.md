# MDviewer Git 및 변경 이력 관리 지침 (git.md)

본 문서는 **MDviewer** 프로젝트의 Git 형상 관리와 변경 이력(`History.md`) 작성에 관한 표준 원칙을 정의합니다.
**이 프로젝트를 작업하는 모든 AI 어시스턴트(에이전트)와 개발자는 변경 사항을 Git에 커밋/등록할 때 반드시 본 지침을 준수해야 합니다.**

---

## 📌 핵심 원칙 (Core Rules)

### 1. Git 커밋 전 `History.md` 업데이트 필수
- 소스 코드 수정, 기능 추가, UI 개선, 버그 수정 등 작업을 완료하고 Git에 커밋/등록하기 전에 **반드시 `History.md` 파일에 변경 내역을 기록**해야 합니다.
- 변경 이력은 **작업 당일 날짜 (`YYYY-MM-DD`)**를 기준으로 기록합니다.
- 최신 날짜가 문서 상단에 위치하는 **역연대순**으로 정리합니다.
- 동일한 날짜에 여러 번의 작업이 진행되는 경우, 해당 날짜 섹션 아래에 내용을 추가/보강합니다.

---

## 📝 `History.md` 작성 표준 포맷

각 날짜별 섹션은 아래와 같은 구조로 작성합니다:

```markdown
## YYYY-MM-DD

### 추가 (Added)
- 신규 기능, 새로운 메뉴/옵션, 신규 단축키, 신규 파일 등

### 변경 및 개선 (Changed)
- 기존 기능의 동작 변경, 성능 개선, UI/UX 개선, 폰트/레이아웃 변경 등

### 수정 (Fixed)
- 버그 수정, 런타임 예외 처리, 비정상 동작 수정 등

### 삭제 (Removed)
- 더 이상 사용되지 않아 제거된 기능 또는 레거시 코드
```

---

## 🔄 문서 및 빌드 동기화 규칙

1. **`README.md` 동기화**:
   - 단축키, 마우스 조작법, 서브 메뉴 항목, 주요 기능이 변경되거나 추가된 경우 `README.md`의 해당 설명도 반드시 함께 업데이트합니다.
2. **배포 바이너리 빌드 확인**:
   - 기능 변경 완료 후 `build.bat` 또는 `dotnet publish`를 통해 `publish\MDviewer.exe` 단일 실행 파일이 에러 없이 정상 생성되는지 검증합니다.
3. **History.md 커밋 포함**:
   - `History.md` 변경 내역은 코드 변경 파일과 함께 하나의 커밋(또는 연관 커밋)으로 묶어 등록합니다.

---

## 💬 Git 커밋 메시지 컨벤션

커밋 메시지는 변경 사항의 성격을 명확히 알 수 있도록 아래 접두사를 사용합니다:

- `feat:` 새로운 기능 추가
- `fix:` 버그 및 오류 수정
- `docs:` 문서 수정 (`README.md`, `History.md`, `git.md` 등)
- `style:` UI 디자인, 폰트 크기, 색상, 레이아웃 스타일 변경
- `refactor:` 코드 구조 개선 및 리팩토링
- `chore:` 빌드 스크립트, 설정 파일 변경
